#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * rot13 - encodes a string using rot13
 *
 * @s: string to encode
 *
 * Return: encoded string
 */
char *rot13(char *s)
{
	return (s);
}
